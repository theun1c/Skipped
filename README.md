Skipped - missed classes tracker




### полезное 

#### на локалке стрем, поэтому поднимаем контейнер фастом 
docker run --rm -it -v ${PWD}:/workspace -w /workspace ruby:3.2 bash

#### чтоб сохранить гемфайл
sudo chown username -R dir (dir на точку заменить чтоб не париться)